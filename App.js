import React from 'react';
import {SafeAreaView} from 'react-native';
import App from './src/root';
const BoomSpots = () => {
  return (
    <SafeAreaView>
      <App />
    </SafeAreaView>
  );
};

export default BoomSpots;
